# ideal-gas-simulation
This code implements a simple Euler step for point particles which are randomly positioned inside a box (with periodic boundary conditions) with momenta sampled from a unit sphere. The output is a collision array (an NxN array storing the time at which particles i and j pass within two radii of one another). No momentum updates. 
